'use strict';

const React = require('react');

const ManageMonitorPage = React.createClass({
    render: () => {
        return (
            <div></div>
        );
    }
});

module.exports = ManageMonitorPage;
