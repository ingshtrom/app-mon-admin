'use strict';

const React = require('react');

const MonitorPage = React.createClass({
    render: () => {
        return (
            <div>
                
            </div>
        );
    }
});

module.exports = MonitorPage;
